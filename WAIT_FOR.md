# WAIT_FOR —— 待补全功能清单

本文档记录当前实现中为了「先跑起来」而采取的简化和空壳，以及将来要补全的方向。

## 数组功能（基础 + 多维/多值已实现）

当前已支持：

- 数组声明 `数组 数组名[长度] = {内容}`，长度可为任意表达式（动态长度）
- 多维数组声明 `数组 矩阵[行][列] = {...}`，扁平 row-major 存储
- 初始化列表三种形态：
  - 单标量广播：`数组 示例[3] = {7}` → `[7,7,7]`
  - 扁平列表：`{1,2,3}`，短于长度补 0、长于长度报错
  - 嵌套列表：`{{1,2,3},{4,5,6}}`，深度 == 维度数，每层短补 0、长报错
- 声明时省略初始化：`数组 示例[3]` → 默认全 0
- 数组读取 `数组名[索引]` / 写入 `数组名[索引] = 内容`
- 多维访问 `矩阵[i][j]`（变长下标指令 ARRGETN/ARRSETN），经函数参数等不透明句柄访问同样正确
- 连缀索引 `外[i][j]`：元素为数组句柄时可直接多层读取/写入
- 数组作为函数参数传递，引用语义（调用方与函数内共享同一份数组）
- 越界 / 负下标抛运行时错误 `数组下标越界`
- 元素可为整数或字符串
- 数组类型系统：`arrayRank` 记录在符号表；标量被索引编译期报错、rank≥2 下标数量编译期检查
- 编译期常量长度检查：常量长度 > 0 编译期报错，动态长度运行期兜底

将来要补全的功能：

### 语法与语义
- [ ] 数组整体赋值 / 深拷贝语义选项

### 优化流水线（TAC / 空壳）
- [ ] TAC 优化 pass 对数组指令的识别与优化
  - 当前 ConstantPropagation 与 DCE 仅将数组指令视为黑盒（保护其引用寄存器不被误删、结果寄存器不做常量折叠），不做任何数组相关优化
- [ ] 数组越界检查消除（当索引为编译期常量且在 [0, 长度) 内时，跳过运行期检查）
- [ ] 数组长度/常量传播、数组访问的 LICM、循环展开
- [ ] 函数内联对含数组指令函数的支持（当前 FunctionInlining 等 pass 已注释未启用）

### JIT
- [ ] JIT 编译数组指令（ARRNEW / ARRGET / ARRSET / ARRDIMSET / ARRGETN / ARRSETN）
  - 当前 `canJIT` 遇到含数组指令的函数直接返回 false，整函数退回解释器
  (This is KrisPoint.I think its useless.because example/calc_fib_array.rua just took 8 微秒).
- [ ] 数组作为 JIT 函数参数 / 返回值的支持

### VM 运行时
- [ ] 数组长度运行时上限 / 数组池回收（目前数组池随程序执行单调增长，无回收）
- [ ] 数组打印：`喵叫(示例)` 目前输出 `(数组)`，将来可打印元素序列

---

最后更新：2026-08-12

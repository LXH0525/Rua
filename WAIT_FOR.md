# WAIT_FOR —— 待补全功能清单

本文档记录当前实现中为了「先跑起来」而采取的简化和空壳，以及将来要补全的方向。

## 数组功能（已支持基础，优化为空壳）

当前已支持：

- 数组声明 `数组 数组名[长度] = {内容}`（长度必须为正整数字面量，`{}` 单值广播填充）
- 数组读取 `数组名[索引]`（索引可为任意表达式，运行期求值）
- 数组写入 `数组名[索引] = 内容`
- 连缀索引 `数组名[i][j]`：元素为数组句柄时可直接多层读取/写入（如 `数组 外[2] = {内}` 后 `外[0][1]`）
- 数组作为函数参数传递，引用语义（调用方与函数内共享同一份数组）
- 越界 / 负下标抛运行时错误 `数组下标越界`
- 元素可为整数或字符串

将来要补全的功能：

### 语法与语义
- [ ] 多值初始化列表：`数组 示例[3] = {1, 2, 3}`，列表短于长度补 0、长于长度报错
- [ ] 动态长度：`数组 示例[变量] = {0}`，长度允许任意表达式
- [ ] 声明时省略初始化：`数组 示例[3]`
- [ ] 多维数组字面量声明：`数组 矩阵[3][3] = {0}`（目前多维访问须先声明「元素为数组的数组」，如 `数组 外[2] = {内}` 再 `外[i][j]`）
- [ ] 数组类型系统：`数组` 作为独立类型参与符号表检查，编译期区分数组与标量
  - 目前 `isArray` 标记已记录在符号表中，但索引检查放宽为「仅检查变量已声明」，非数组被索引由运行时 ARRGET/ARRSET 兜底抛出 `索引的目标不是数组`
- [ ] 编译期常量长度检查（长度必须是正数已在语义分析阶段检查）
- [ ] 数组整体赋值 / 深拷贝语义选项

### 优化流水线（TAC / 空壳）
- [ ] TAC 优化 pass 对数组指令的识别与优化
  - 当前 ConstantPropagation 与 DCE 仅将数组指令视为黑盒（保护其引用寄存器不被误删、结果寄存器不做常量折叠），不做任何数组相关优化
- [ ] 数组越界检查消除（当索引为编译期常量且在 [0, 长度) 内时，跳过运行期检查）
- [ ] 数组长度/常量传播、数组访问的 LICM、循环展开
- [ ] 函数内联对含数组指令函数的支持（当前 FunctionInlining 等 pass 已注释未启用）

### JIT
- [ ] JIT 编译数组指令（ARRNEW / ARRGET / ARRSET）
  - 当前 `canJIT` 遇到含数组指令的函数直接返回 false，整函数退回解释器
- [ ] 数组作为 JIT 函数参数 / 返回值的支持

### VM 运行时
- [ ] 数组长度运行时上限 / 数组池回收（目前数组池随程序执行单调增长，无回收）
- [ ] 数组打印：`喵叫(示例)` 目前输出 `(数组)`，将来可打印元素序列

---

最后更新：2026-08-01
